{
  "meta": {
    "title": "SuntiQ Goal Package — Deep Reference",
    "description": "Owner rulings, architecture maps, decision briefs, receipt mechanics, DTU identity evidence, and the full P0 dossier behind the approved sim-parity plan.",
    "layout": "sidebar-toc",
    "effects": ["title-underline", "grid-dots"]
  },
  "theme": { "preset": "deep-blue-gold", "mode": "dark" },
  "root": ["hero", "kpis", "rulings-section", "arch-c1-section", "arch-c2-section", "arch-ingest-section", "arch-auth-section", "plan-section", "decisions-index-section", "adr78-section", "receipts-section", "dtu-section", "disease1-section", "disease2-section", "structural-section", "slice-section", "next-section"],
  "elements": {
    "hero": {
      "type": "hero",
      "variant": "editorial",
      "props": {
        "category": "APPROVED PLAN · RULINGS CAPTURED · SLICE EMITTED",
        "title": "SuntiQ fleet: the deep reference",
        "subtitle": "Everything behind the 94/100 approved package — architecture, decisions, receipt mechanics, the DTU identity evidence, and all thirteen audit findings with file-level detail.",
        "meta": "2026-07-18 · fleet-platform-architecture workstream · updated after owner rulings"
      },
      "children": ["hero-lead"]
    },
    "hero-lead": {
      "type": "text",
      "variant": "lead",
      "props": { "content": "Since the first render: four of the five owner questions are ruled and written into the plan; the first execution slice (wire-deploy-path) is emitted and validates READY; and a vendor-doc inspection of the DTU answered the identity question — port-per-box is NOT the only option. This page is the full brief on each of those, plus the expanded disease-by-disease bug dossier." }
    },

    "kpis": {
      "type": "section",
      "variant": "kpi-row",
      "props": { "label": "STATE", "title": "Where things stand" },
      "children": ["kpi-score", "kpi-rulings", "kpi-slice", "kpi-p0", "kpi-identity", "kpi-blockers"]
    },
    "kpi-score": { "type": "card", "variant": "kpi", "props": { "value": 94, "label": "Adversarial review score /100", "trend": { "direction": "up", "value": "from 69" } } },
    "kpi-rulings": { "type": "card", "variant": "kpi", "props": { "value": "4 / 5", "label": "Owner questions ruled" } },
    "kpi-slice": { "type": "card", "variant": "kpi", "props": { "value": 1, "label": "Task packet emitted (validates READY)" } },
    "kpi-p0": { "type": "card", "variant": "kpi", "props": { "value": 10, "label": "P0 findings, all file-pinned" } },
    "kpi-identity": { "type": "card", "variant": "kpi", "props": { "value": 3, "label": "Viable 20-box identity mechanisms" } },
    "kpi-blockers": { "type": "card", "variant": "kpi", "props": { "value": 0, "label": "Open plan blockers" } },

    "rulings-section": {
      "type": "section",
      "variant": "full-width",
      "props": { "label": "OWNER RULINGS", "title": "Your four rulings, now in canon", "subtitle": "Captured into the plan's fitness functions and the workstream ledger. One question stays open." },
      "children": ["rulings-table", "rulings-note"]
    },
    "rulings-table": {
      "type": "data-table",
      "props": {
        "columns": [
          { "key": "q", "label": "Question" },
          { "key": "ruling", "label": "Ruling" },
          { "key": "effect", "label": "What it changes" }
        ],
        "rows": [
          { "q": "Parity bar", "ruling": { "text": "Full 10-scenario recipe set", "status": "match" }, "effect": "The oracle covers scenario × surface (~10 × ~12 cells). Fitness function updated: ALL ten fleet-ai-simulator recipes must pass through cloud ingest, not a demo script." },
          { "q": "Legacy history at launch", "ruling": { "text": "Launch with watermark", "status": "match" }, "effect": "Every history-backed surface shows \"history begins <cutover date>\". Evidence-grade readers (insurance, debriefs) fail closed below the watermark. Later backfill must not change already-issued reports." },
          { "q": "Sim fleet seed", "ruling": { "text": "Multi-org with 1-boat shadows", "status": "match" }, "effect": "Main dev org holds the full sim fleet; each shadow org holds one boat. Purpose: the oracle asserts cross-tenant invisibility per surface — this pins the per-feature multi-tenant e2e suite that the steal-list plans already describe." },
          { "q": "Admin flavor in parity bar", "ruling": { "text": "Operator build only", "status": "match" }, "effect": "Parity declares on operator surfaces. The admin flavor follows its own track after roles-as-data, per the admin-build-flavor decision's sequencing." },
          { "q": "Clerk scope", "ruling": { "text": "STILL OPEN", "status": "warn" }, "effect": "Dev instance is the working default — slice 1 proceeds on it. The production-instance + fleet.suntiq.ai DNS call waits." }
        ]
      }
    },
    "rulings-note": {
      "type": "text",
      "variant": "prose",
      "props": { "content": "The multi-org ruling is the one with hidden depth: a 1-boat shadow org is the cheapest possible tenancy probe. If any surface — map, dispatch, insurance, network — ever shows the shadow boat to the main org (or vice versa), that's a per-surface tenancy failure caught by the same oracle run that checks scenario states. Tenancy testing stops being a separate initiative and becomes a free by-product of every parity iteration." }
    },

    "arch-c1-section": {
      "type": "section",
      "variant": "default",
      "props": { "label": "ARCHITECTURE · C1", "title": "System context", "subtitle": "Who touches what. The fleet product and the website are deliberately separate systems with separate Convex backends." },
      "children": ["arch-c1-diagram", "arch-c1-prose"]
    },
    "arch-c1-diagram": {
      "type": "diagram",
      "props": {
        "engine": "mermaid",
        "caption": "C1 — system context",
        "content": "flowchart LR\n  FO[\"Fleet operators\"]\n  OA[\"Organisation admins\"]\n  BH[\"Boat hardware<br/>NMEA 2000 + DTU\"]\n  IV[\"Investors and data-room visitors\"]\n  SA[\"Platform super-admin\"]\n  FLEET[\"Fleet SaaS<br/>operator command center\"]\n  WEB[\"SuntiQ website<br/>and investor data room\"]\n  PIPE[\"DTU telemetry pipe<br/>4G ingest and decoding\"]\n  DATA[\"GCP archive and analytics<br/>planned cold-data tier\"]\n  THIRD[\"Third parties<br/>auth, email, CRM, AI, maps, analytics\"]\n  FO --> FLEET\n  OA --> FLEET\n  SA --> FLEET\n  IV --> WEB\n  BH --> PIPE\n  PIPE --> FLEET\n  FLEET -. archive before prune, when enabled .-> DATA\n  FLEET <--> THIRD\n  WEB <--> THIRD\n  WEB --> FLEET"
      }
    },
    "arch-c1-prose": {
      "type": "text",
      "variant": "prose",
      "props": { "content": "The fleet dashboard is the operational system of record; the website owns marketing, leads, authentication, and the data room. GCP never replaces Convex — it is the durable archive and analytics tier (the data-placement decision), which is exactly why the receipt mechanics below exist: nothing may be pruned from the live system until the archive tier has provably received it." }
    },

    "arch-c2-section": {
      "type": "section",
      "variant": "default",
      "props": { "label": "ARCHITECTURE · C2", "title": "Containers and deployment boundaries", "subtitle": "The boxes that actually run, and who owns each one." },
      "children": ["arch-c2-diagram", "arch-c2-prose"]
    },
    "arch-c2-diagram": {
      "type": "diagram",
      "props": {
        "engine": "mermaid",
        "caption": "C2 — containers",
        "content": "flowchart TB\n  subgraph KINGLY[\"Kingly / SuntiQ cloud target\"]\n    VF[\"Vercel: fleet project<br/>team kinglyai\"]\n    VW[\"Vercel: website project\"]\n    CF[\"Fleet Convex<br/>suntiq-fleet — existence verified<br/>by command, never assumed\"]\n    CW[\"Website Convex\"]\n    CLERK[\"Clerk fleet auth<br/>dev instance = owner handoff\"]\n    BA[\"Better Auth<br/>website authentication\"]\n    GCP[\"GCP target<br/>GCS raw archive + BigQuery later\"]\n  end\n  subgraph BOAT[\"Boat and ingest\"]\n    DTU[\"ChuangXin CAN/CANFD-4G DTU<br/>fixed firmware, ~$70\"]\n    VM[\"DigitalOcean ingest VM<br/>143.110.152.11:5001<br/>systemd dtu · DRY_RUN=1\"]\n  end\n  DTU -->|\"Format-1 raw TCP over 4G\"| VM\n  VM -->|\"tracking.reportTelemetry\"| CF\n  VF <-->|\"reactive queries and mutations\"| CF\n  VF <-->|\"JWT template convex\"| CLERK\n  VW <--> CW\n  VW <--> BA\n  CF -. \"scheduled archive before prune\" .-> GCP"
      }
    },
    "arch-c2-prose": {
      "type": "text",
      "variant": "prose",
      "props": { "content": "Two facts on this map drive the whole plan. First, the ingest VM still runs in dry-run mode — it decodes and logs but writes nothing, which is why cutover is low-risk. Second, the fleet Convex deployment box says \"verified by command, never assumed\": an earlier session recorded a deployment id that turned out to be fabricated, so the emitted slice makes deployment-existence-by-command-receipt its first acceptance criterion." }
    },

    "arch-ingest-section": {
      "type": "section",
      "variant": "default",
      "props": { "label": "ARCHITECTURE · C4", "title": "Critical path: boat to dashboard", "subtitle": "The production telemetry pipe, frame by frame." },
      "children": ["arch-ingest-diagram", "arch-ingest-prose"]
    },
    "arch-ingest-diagram": {
      "type": "diagram",
      "props": {
        "engine": "mermaid",
        "caption": "DTU telemetry ingest",
        "content": "sequenceDiagram\n  participant BUS as NMEA 2000 bus\n  participant DTU as ChuangXin DTU\n  participant VM as DO VM server-fleet.js\n  participant N2K as n2k.js decoder\n  participant CVX as Fleet Convex tracking.reportTelemetry\n  participant UI as Fleet dashboard live query\n  BUS->>DTU: CAN traffic\n  DTU->>VM: Format-1 frames over 4G raw TCP on :5001\n  VM->>VM: buffer partial frames, validate channel/DLC framing\n  VM->>N2K: parse extended CAN ID, reassemble fast packets\n  N2K-->>VM: decode PGNs 129025 129026 127250 128267 127508 129029\n  VM->>VM: merge decoded state, retain raw capture .bin\n  alt DRY_RUN=1 (current)\n    VM-->>VM: decode and log only, no Convex write\n  else DRY_RUN disabled\n    VM->>CVX: POST /api/mutation with boatCode + fields + deviceToken\n    CVX->>UI: org-scoped live view via vessels.code join\n  end"
      }
    },
    "arch-ingest-prose": {
      "type": "text",
      "variant": "prose",
      "props": { "content": "The ingest never invents telemetry: on a framing desync it saves the raw bytes and stops that buffer; out-of-range values are dropped in the decoder; a failed Convex push retries. The one architectural flaw is at the top — the whole process is stamped with a single BOAT_CODE environment variable, which is the 20-box hard blocker the data-scale design exists to fix (`dtu-ingest/server-fleet.js:46,69`)." }
    },

    "arch-auth-section": {
      "type": "section",
      "variant": "default",
      "props": { "label": "ARCHITECTURE · C4", "title": "Critical path: authentication and tenancy", "subtitle": "Why the browser must be a projection — the server-side choke point every screen is supposed to respect." },
      "children": ["arch-auth-diagram", "arch-auth-prose"]
    },
    "arch-auth-diagram": {
      "type": "diagram",
      "props": {
        "engine": "mermaid",
        "caption": "Fleet auth and tenancy",
        "content": "sequenceDiagram\n  participant U as Fleet user or admin\n  participant C as Clerk\n  participant A as AuthGate + ConvexProviderWithClerk\n  participant X as Fleet Convex auth.config\n  participant T as requireUser / requireOrg\n  participant R as Org-scoped rows\n  U->>C: sign in\n  C-->>A: Clerk JWT from template convex\n  A->>X: authenticated request with JWT\n  X->>X: validate issuer + audience\n  X->>T: handler requests an org action\n  T->>T: resolve subject to user + membership\n  alt member of requested org\n    T->>R: read/mutate only that org's rows\n    R-->>A: org-scoped result\n  else not a member\n    T-->>A: reject before any org-scoped access\n  end"
      }
    },
    "arch-auth-prose": {
      "type": "text",
      "variant": "prose",
      "props": { "content": "This diagram is the reference point for the entire disease-one section below. The server holds the only real authority: membership, roles, locks, zones, acknowledgements. Every P0 in that cluster is a place where a screen kept its own local copy of one of those facts and let it diverge from this chain — the app's own code comments call the rule \"the browser is a projection,\" and the bugs are exactly the places that rule is broken." }
    },

    "plan-section": {
      "type": "section",
      "variant": "default",
      "props": { "label": "THE PLAN", "title": "Sim-parity in five steps", "subtitle": "The execution slice of the parallel-infrastructure migration, extended to the real target: fleet.suntiq.ai 1:1 with all ten AI simulation scenarios." },
      "children": ["plan-timeline", "plan-contract-diagram", "plan-prose"]
    },
    "plan-timeline": {
      "type": "timeline",
      "props": {
        "items": [
          { "date": "Step 1", "title": "Wire the deploy path — EMITTED", "status": "in-progress", "description": "ArgoFleet mirror → SuntiQ Vercel → verified suntiq-fleet Convex. Exit: staging renders cloud mode with an empty org. This is the task packet emitted today; it validates READY and waits only on your Clerk dev keys." },
          { "date": "Step 2", "title": "Rename pass on the fleet surface", "status": "upcoming", "description": "Brand strings and assets Argo→SuntiQ in the dashboard; domain references to fleet.suntiq.ai. Partner-owned repo and deployment identifiers stay until their own cutovers." },
          { "date": "Step 3", "title": "Sim feed + provenance rail", "status": "upcoming", "description": "The prerequisites nothing in step 4 may start without: server-owned source labeling (dtu / edge / sim), the simulator connected through the REAL ingest wire contract, and receipts correlating generator manifests to ingest acknowledgements." },
          { "date": "Step 4", "title": "Parity loop", "status": "upcoming", "description": "Build the per-screen oracle for all ten scenarios (owner ruling), gate evidence readers on permitted sources, replay scenario packs, walk every operator surface, fix what disagrees. The ten P0s land here. Repeat per merge until every fitness function passes." },
          { "date": "Step 5", "title": "Exit review", "status": "upcoming", "description": "Record parity evidence; hand off to the 20-box readiness design for real-data scale." }
        ]
      }
    },
    "plan-contract-diagram": {
      "type": "diagram",
      "props": {
        "engine": "mermaid",
        "caption": "The acceptance contract — how a scenario proves itself",
        "content": "flowchart LR\n  GEN[\"Simulator generator<br/>scenario pack + manifest<br/>run id, boat, track, expected states\"]\n  ING[\"Production ingest<br/>tracking.reportTelemetry<br/>sim-lane service key\"]\n  SRV[\"Server assigns<br/>source=sim + ingestId<br/>never trusted from payload\"]\n  HIST[\"History tier<br/>append-only rows<br/>carrying source=sim\"]\n  ORA[\"Per-screen oracle<br/>expected state per surface<br/>incl. permitted data-starved states\"]\n  R1[\"Receipt 1<br/>generator manifest\"]\n  R2[\"Receipt 2<br/>ingest acks\"]\n  R3[\"Receipt 3<br/>oracle run record\"]\n  GEN --> ING --> SRV --> HIST --> ORA\n  GEN -.-> R1\n  SRV -.-> R2\n  ORA -.-> R3\n  R1 --- R2 --- R3"
      }
    },
    "plan-prose": {
      "type": "text",
      "variant": "prose",
      "props": { "content": "The done-condition is deliberately falsifiable: a labeled scenario pack enters through the production-equivalent ingest (never a parallel seam), its synthetic provenance stays visible through storage and every evidence output, and a per-screen oracle defines the expected state for every operator surface — including legitimately empty \"collecting data\" states. The three receipts correlate by run id and ingest ids; a run missing any link proves nothing. Crucially the oracle carries *negative* assertions: an insurance report that renders the sim trip as real evidence is a contract FAIL." }
    },

    "decisions-index-section": {
      "type": "section",
      "variant": "full-width",
      "props": { "label": "DECISIONS", "title": "The decision record, one line each", "subtitle": "Eight architecture decisions govern the fleet platform. The two newest (raw-capture-first and append-only) are detailed below." },
      "children": ["decisions-table"]
    },
    "decisions-table": {
      "type": "data-table",
      "props": {
        "columns": [
          { "key": "adr", "label": "ADR" },
          { "key": "status", "label": "Status" },
          { "key": "decision", "label": "Decision in one line" }
        ],
        "rows": [
          { "adr": "001 — Map runtime", "status": { "text": "accepted", "status": "match" }, "decision": "Leaflet stays production authority; a MapLibre/deck.gl candidate promotes only behind a feature flag after parity evidence." },
          { "adr": "002 — HUD shell", "status": { "text": "accepted", "status": "match" }, "decision": "Build the domain UI library before adopting the command-HUD shell; the shell remains proposed." },
          { "adr": "003 — AI command authority", "status": { "text": "accepted", "status": "match" }, "decision": "AI narrative and typed proposals stay subordinate to deterministic authorization, validation, and execution." },
          { "adr": "004 — Repo extraction", "status": { "text": "accepted", "status": "match" }, "decision": "Extract fleet-dashboard via deterministic subtree split into standalone ArgoFleet; re-added as fleet/ at hard cutover." },
          { "adr": "005 — Data placement", "status": { "text": "accepted", "status": "match" }, "decision": "Convex is the live system of record; GCS + BigQuery are the archive/analytics tier. Its open archive-timing tension is what ADR-007 resolves." },
          { "adr": "006 — Admin flavor + isolation", "status": { "text": "accepted", "status": "match" }, "decision": "Same-repo admin build flavor; server-side requirePlatformAdmin is the real boundary; row-level tenancy by default, per-tenant infra only as a contract-triggered premium tier." },
          { "adr": "007 — Raw capture first", "status": { "text": "proposed", "status": "info" }, "decision": "Archive raw CAN bytes to GCS from day one — the RAW tier archives NOW, the decoded/analytics tier stays trigger-gated. This pins the first GCP resource on a trivially small workload." },
          { "adr": "008 — Append-only telemetry", "status": { "text": "proposed", "status": "info" }, "decision": "Live tier is a mutable cache; history tier is append-only with server-owned provenance; pruning requires a DECODED archive receipt." }
        ]
      }
    },

    "adr78-section": {
      "type": "section",
      "variant": "comparison",
      "props": { "label": "DECISION BRIEFS", "title": "The two new decisions, in full", "subtitle": "Both are drafts awaiting your acceptance. Both were adversarially reviewed and repaired across four cycles." },
      "children": ["adr7-card", "adr8-card"]
    },
    "adr7-card": {
      "type": "card",
      "variant": "elevated",
      "props": { "title": "📼 Raw telemetry capture first (ADR-007)", "content": "**Problem:** the data-placement decision left a tension open — start the GCP archive now, or at a ~50-boat trigger? Meanwhile the ingest already writes every connection's raw bytes to `captures/*.bin`… on a 1 GB droplet disk. Twenty boxes are inbound.\n\n**Decision:** split the tension by tier. The RAW tier archives NOW: rotate the existing capture files hourly → gzip → `gs://suntiq-telemetry-raw/raw/boat=<code>/date=<d>/`, written by a create-only service account (no delete permission), lifecycle to cold storage at 30 days. Parquet and BigQuery stay out of scope until the decoded-tier trigger.\n\n**Why it matters:** raw CAN is the only loss-proof representation. Decoded rows inherit every decoder bug; raw can always be re-decoded when a new PGN or a decoder fix lands. This tiny pipeline is also deliberately the first SuntiQ GCP resource — project, billing, and IAM bootstrap happen on a workload where nothing can go badly wrong.\n\n**Consequence:** ground truth survives VM loss from box #1 onward, and every later data decision becomes reversible." }
    },
    "adr8-card": {
      "type": "card",
      "variant": "elevated",
      "props": { "title": "📜 Append-only telemetry history (ADR-008)", "content": "**Problem:** telemetry lands via an upsert; history rows are not structurally immutable; retention pruning predates any archive guarantee. ML training, debriefs, benchmarks, insurance evidence, and the simulator's honesty path all assume history that cannot be silently rewritten.\n\n**Decision — a two-tier contract:** the LIVE tier is one mutable snapshot row per boat (a cache — upserts are fine). The HISTORY tier is append-only: every accepted report inserts an immutable event (boat, time, fields, source, ingest id for idempotent replay). No mutation outside the archiver may update or delete a history row.\n\n**Provenance is server-owned**, classified by what each client can actually prove — the box-from-China section below explains the dtu case. A report whose origin cannot be resolved is rejected or quarantined as unverified, never defaulted to real.\n\n**Migration honesty:** writers are additive, but readers are NOT assumed unchanged — the cutover requires a dataset authority contract: the legacy/new write boundary and completeness watermark get recorded; each consumer declares its permitted sources (real-only, sim-permitted, mixed); evidence-grade readers fail closed below the watermark; and every history reader gets a semantic audit so a late backfill cannot silently change previously issued aggregates. Your launch-with-watermark ruling plugs directly into this contract." }
    },

    "receipts-section": {
      "type": "section",
      "variant": "default",
      "props": { "label": "RECEIPT MECHANICS", "title": "Two receipt types, one hard rule", "subtitle": "Why a raw backup can never authorize deleting decoded history — the invariant that makes archive-before-prune mechanical instead of aspirational." },
      "children": ["receipts-diagram", "receipts-compare", "receipts-rule"]
    },
    "receipts-diagram": {
      "type": "diagram",
      "props": {
        "engine": "mermaid",
        "caption": "RAW and DECODED receipt flows",
        "content": "flowchart TB\n  subgraph RAWPATH[\"RAW tier — ADR-007\"]\n    CAP[\"captures/*.bin<br/>raw bytes per connection\"]\n    GZ[\"hourly rotate + gzip\"]\n    GCS[\"GCS raw bucket<br/>create-only service account\"]\n    RR[\"RAW receipt<br/>object path + byte checksum<br/>+ capture window + boat\"]\n    CAP --> GZ --> GCS --> RR\n  end\n  subgraph DECPATH[\"DECODED tier — ADR-008\"]\n    HIST[\"history rows<br/>append-only events\"]\n    ARC[\"archiver<br/>decoded archive object\"]\n    DR[\"DECODED receipt<br/>archive object + row count<br/>+ ingestId-range match\"]\n    PRUNE[\"retention prune<br/>deletes matched rows ONLY\"]\n    HIST --> ARC --> DR --> PRUNE\n  end\n  RR -. \"proves raw durability ONLY<br/>can NEVER authorize row deletion\" .-x PRUNE"
      }
    },
    "receipts-compare": {
      "type": "section",
      "variant": "comparison",
      "props": { "label": "", "title": "" },
      "children": ["receipt-raw-card", "receipt-decoded-card"]
    },
    "receipt-raw-card": {
      "type": "card",
      "variant": "glass",
      "props": { "title": "RAW receipt — what it proves", "content": "One receipt per shipped capture object: GCS object path, byte checksum, capture time window, boat. It proves exactly one thing — the raw byte stream for that window is durably stored off the VM.\n\nIt says nothing about decoded rows, because the mapping raw→rows is not one-to-one: the decoder drops out-of-range values, reassembles multi-frame messages, and one frame can fan out into several fields. A raw file can be perfectly archived while the decoded rows derived from it are wrong, missing, or duplicated." }
    },
    "receipt-decoded-card": {
      "type": "card",
      "variant": "glass",
      "props": { "title": "DECODED receipt — what it authorizes", "content": "Produced by the archiver when it exports history rows: archive object, row count, and the ingest-id range, all matched back against the history tier before the receipt is recorded.\n\nOnly this receipt authorizes retention pruning, and only for the exact rows it names. The deletion code path takes a receipt reference, not a date range — which means \"archive before prune\" stops being a convention someone remembers and becomes a precondition the code cannot skip." }
    },
    "receipts-rule": {
      "type": "card",
      "variant": "callout",
      "props": { "variant": "warning", "title": "The rule that survived adversarial review", "content": "Each deletion class must name its authorizing receipt type. The reviewer's original finding was that the first draft conflated the two receipts — under which a raw backup would have quietly licensed deleting decoded history that was never actually archived. The split is now explicit in both decision documents." }
    },

    "dtu-section": {
      "type": "section",
      "variant": "default",
      "props": { "label": "DTU DEEP DIVE", "title": "Is port-per-box really the only option? No.", "subtitle": "A vendor-doc inspection this session found two documented in-stream identity mechanisms in the appliance's own protocol suite. Here is the evidence and what it changes." },
      "children": ["dtu-context", "dtu-options-table", "dtu-format5-card", "dtu-format80-card", "dtu-spike-card", "dtu-verdict"]
    },
    "dtu-context": {
      "type": "text",
      "variant": "prose",
      "props": { "content": "The blocker: `server-fleet.js` stamps every TCP connection with one BOAT_CODE environment variable (`dtu-ingest/server-fleet.js:46,69`). Twenty boxes hitting port 5001 would all write as the same vessel. The appliance is fixed-firmware — you edit a `config.ini` over USB, you cannot run code on it — so identity must come from network arrangement or from what the firmware already emits.\n\nThe design committed to port-per-box as the deployable baseline (twenty listener ports, one per vessel, configured during the hand USB setup each box needs anyway). That decision stands. What changed today is the quality of the alternative: the \"stream identity\" option went from *speculative* to *documented*." }
    },
    "dtu-options-table": {
      "type": "data-table",
      "props": {
        "columns": [
          { "key": "opt", "label": "Option" },
          { "key": "how", "label": "How it works" },
          { "key": "verdict", "label": "Verdict after evidence" }
        ],
        "rows": [
          { "opt": { "text": "A · Port-per-box", "status": "match" }, "how": "20 systemd instances, each PORT + BOAT_CODE; each box's config.ini points at its own port.", "verdict": "COMMITTED BASELINE. Zero code, zero firmware risk. Ugly at 20, unmanageable at 100 — an honest bridge." },
          { "opt": { "text": "B · Source IP mapping", "status": "gap" }, "how": "Map the connection's remote address to a boat.", "verdict": "REJECTED. Cellular carrier-grade NAT makes source addresses shared and unstable." },
          { "opt": { "text": "C · In-stream identity (Format 5)", "status": "info" }, "how": "Set [4G] Format = 5 in config.ini: the firmware prepends the device serial's last four digits to EVERY frame. Single port; server maps SN→boat.", "verdict": "NOW DOCUMENTED — promoted from speculation to a concrete, cheap spike. One open check: SN last-4 uniqueness across all 21 devices." },
          { "opt": { "text": "D · Per-box containers + LB", "status": "gap" }, "how": "One ingest container per box behind a load balancer.", "verdict": "REJECTED. Overkill at this scale." },
          { "opt": { "text": "(E) · Cloud protocol (Format 80)", "status": "warn" }, "how": "Full framed protocol with custom 16-char serial, register/auth/login/heartbeat frames.", "verdict": "DO NOT BUILD ON IT. The protocol doc is stamped unpublished and those frame types carry no completion mark — assume absent from shipped firmware." }
        ]
      }
    },
    "dtu-format5-card": {
      "type": "card",
      "variant": "elevated",
      "props": { "title": "📡 The Format 5 evidence", "content": "The vendor's frame-format definition (`dtu-ingest/vendor-docs/4G记录仪消息帧格式定义20250416.pdf`) specifies five wire formats. Format 1 is what we run today. **Format 5 is defined as \"Format 1 with the last four digits of the SN prepended\"** — a 4-character string (e.g. \"1234\") before every frame, worked example included in the doc.\n\nThat means identity arrives with the very first telemetry frame, survives reboot and reconnect by construction (it's the hardware serial), and requires no protocol handshake the firmware might not implement. It is selectable today: the factory `config.ini` exposes `[4G] Format` with documented values 1–6 and 80; ours currently says 1.\n\nOne consequence for our side: the current frame parser would desync on the extra four bytes, so ingest needs a small Format-5 parser variant — roughly a ten-line change, isolated in the framing layer." }
    },
    "dtu-format80-card": {
      "type": "card",
      "variant": "elevated",
      "props": { "title": "🚫 The Format 80 trap (and why we're avoiding it)", "content": "The cloud-platform protocol (V1.1, June 2025) looks like everything you'd want: 0x55-framed messages with XOR checksums, a configurable custom serial up to 16 characters (`Sn = {1,<boatcode>}` — you could literally put the boat code in the device), plus device register, authenticate, login, logout, and heartbeat frame types.\n\nBut the document is stamped **未发布 — unpublished** — and in its own frame-type table only CAN data report/send and GPS report carry completion marks. Register, authenticate, login, and heartbeat show none. Building our identity scheme on frames the shipped firmware likely doesn't emit would put an unverifiable vendor promise on the critical path — exactly the failure mode the adversarial review kept hammering. It stays on the watch list for a future firmware revision, nothing more.\n\nOne genuinely useful find in the same doc: the protocol defines platform→device CAN *transmission*. Our boxes are configured listen-only (`Mode = 1`) precisely so nothing can ever transmit onto a customer's NMEA 2000 bus — worth keeping in mind as a standing safety property." }
    },
    "dtu-spike-card": {
      "type": "card",
      "variant": "glass",
      "props": { "title": "🔬 The spike, now concrete (bench, box #1)", "content": "1. Set `Format = 5` in config.ini, power from DC, confirm the 4-byte serial prefix appears on the wire.\n2. Confirm the prefix survives reboot and cellular reconnect.\n3. Inventory all 21 devices' serial last-4 for collisions — four digits across 21 devices are likely distinct within a manufacturing batch, but that must be verified, not assumed. A collision demotes just the colliding pair to port-per-box.\n4. Add the Format-5 parser variant to ingest and route by serial→boat map.\n\nThe promotion criteria from the design map cleanly: stable across reconnect ✓ (hardware serial) · available before the first telemetry frame ✓ (every frame) · fail-closed on unknown identity ✓ (unknown serial → capture-only quarantine) · operator-configurable ✗ (factory serial — mitigated by the server-side map, same shape as the port map) · unique per device = the one open check." }
    },
    "dtu-verdict": {
      "type": "card",
      "variant": "callout",
      "props": { "variant": "success", "title": "What ships vs what's next", "content": "Port-per-box still ships for the 20-box arrival — it cannot fail and the boxes need hand configuration regardless. But the Format-5 spike is now cheap and well-defined; if it passes, the twenty listeners collapse to one port with in-stream identity, and the ingress lease/quarantine machinery carries over unchanged. The connection→boat mapping module stays isolated either way, so the swap is a config change, not a rewrite." }
    },

    "disease1-section": {
      "type": "section",
      "variant": "stacked",
      "props": { "label": "DISEASE 1", "title": "The browser pretends to be the server", "subtitle": "Six P0s, one pattern. Every finding below is a screen holding a local copy of a fact the server owns — and letting it diverge." },
      "children": ["disease1-explainer", "d1-disp", "d1-org", "d1-geo1", "d1-geo2", "d1-alrt", "d1-zones"]
    },
    "disease1-explainer": {
      "type": "text",
      "variant": "prose",
      "props": { "content": "**What \"server vs browser\" means here.** The dashboard has two kinds of state. Server state lives in Convex — locks, memberships, zones, alerts — and is what every OTHER device, the audit trail, and the automation crons see. Browser state is a local convenience copy (a component store, localStorage) used to render quickly. The app's architecture rule, written in its own code comments, is that the browser is a *projection*: it may cache server facts but never *be* the authority for them.\n\nThe bug pattern is always one of three shapes: **(1) optimistic write without rollback** — update the local copy, fire the server mutation, and when the server refuses, keep the local copy anyway; **(2) fire-and-forget with fake success** — apply locally, toast \"saved,\" and route the server rejection to a console nobody reads; **(3) local-only authority** — the feature was only ever wired to local state, so the server never hears about it at all. All six findings below decompose into these shapes, which is why the fix is a pattern too: await the server, render what it answered, and surface refusal as refusal." }
    },
    "d1-disp": {
      "type": "card",
      "variant": "elevated",
      "props": { "title": "DISP-01 · A refused unlock still unlocks the boat (shape 1)", "content": "**Where:** `src/live/cloudService.ts:110-124` and `src/screens/DispatchScreen.tsx:303-315, 538-539`.\n\n**Mechanism:** when an operator returns a boat to service, `cloudReturnToService` calls the server mutation — and in its error handler *still applies the local unlock* before re-throwing. The screen catches the error and only shows a toast; it never re-locks. The row's button then reads the local store, so it flips to \"Check out.\" The bitter detail: the LOCK direction in the same file does this correctly — refusal keeps the local state locked. Only the unlock direction fails open.\n\n**What an operator sees:** the server refuses the unlock (say, a role restriction), the operator gets an error toast — and the boat still shows as available on this device. This tablet can now dispatch a vessel every other device correctly shows as locked out of service.\n\n**Fix:** mirror the lock direction's fail-safe — on rejection, do not touch local state; re-render the still-locked truth. Lands in the parity loop; a sim scenario with a server-locked boat trips it on the first replay." }
    },
    "d1-org": {
      "type": "card",
      "variant": "elevated",
      "props": { "title": "ORG-01 · The team-management panel is dead for every customer (shape 3, by accident)", "content": "**Where:** `src/screens/SettingsScreen.tsx:500` → `src/components/OrgPanel.tsx:443` → `src/components/MemberManage.tsx:174, 253-254`.\n\n**Mechanism:** a prop threading hole. The settings screen mounts the org panel without an `orgId`; the org panel internally resolves the active org for its *billing* display but forwards the raw, still-undefined `orgId` prop to the member-management component. That component's guard (`orgId && orgId !== ALL_ORGS`) is therefore permanently false — it renders \"Select an organization to manage its members\" forever. A second, independent hole: no production caller passes the viewer's own role/user id, so even with the org resolved, the component would default the org OWNER to viewer rank and hide the invite/remove/role controls.\n\n**What an operator sees:** Settings shows the org's live billing plan correctly, and three lines below it, the entire Team roles & memberships feature claims no org is selected. Unreachable for every customer in production.\n\n**Fix:** one-line resolution (`orgId ?? billingOrgId`) — the pattern already applied to the pairing panel ten lines down — plus threading the caller's real role and user id from the org listing query." }
    },
    "d1-geo1": {
      "type": "card",
      "variant": "elevated",
      "props": { "title": "GEO-01 · Built-in zone toggles are localStorage theater (shape 3)", "content": "**Where:** `src/screens/GeofencesScreen.tsx:657-663`, `src/live/zonePrefs.ts` (pure localStorage), `convex/geofence.ts:107`.\n\n**Mechanism:** toggling a built-in zone writes a browser localStorage key — nothing else. Server-side breach evaluation short-circuits to an empty list for any cloud org (`if (orgId !== undefined) return []` — line 107), so the four built-in zones are never evaluated for a real club regardless of the toggle. Meanwhile the screen's \"in violation now\" tile is computed by a client-side library from those same zones — so it lights up red for a breach that generates no alert, no notification, no webhook, nothing.\n\n**What an operator sees:** they enable a zone, watch a boat cross it, see a red violation badge — and reasonably believe the fleet is protected. With the dashboard closed, nothing is watching at all.\n\n**Fix:** server-authoritative built-ins — either materialize them as real per-org zone rows at onboarding, or make server breach evaluation consult a per-org enabled map. The toggle writes through Convex; localStorage becomes at most a render cache." }
    },
    "d1-geo2": {
      "type": "card",
      "variant": "elevated",
      "props": { "title": "GEO-02 · Custom zones: fake success toasts (shape 2)", "content": "**Where:** `src/live/cloudZones.ts:177-276` (`warnMirror` swallows every rejection into console.warn), `src/screens/GeofencesScreen.tsx:706-717`, `convex/zonesCloud.ts:104` (admin-gated), `:51-62` (3–64 vertex bounds).\n\n**Mechanism:** every zone create/edit/toggle/delete applies to local state synchronously and toasts \"Zone added\" immediately. The server mutation runs as a detached fire-and-forget whose failure handler only logs to the console. The server, meanwhile, enforces two things the client never checks: admin role, and polygon vertex bounds. So a viewer-role user, or anyone drawing a 70-vertex polygon, gets a full success experience for a zone that was never created.\n\n**What an operator sees:** a rendered, enabled-looking geofence that exists only on this device. On every other device — and for the server-side enforcement that matters — the zone does not exist. No error ever surfaces.\n\n**Fix:** await the mutation before toasting; surface rejection as a visible error; add the missing client-side role gate so non-admins can't draw in the first place (the screen currently has zero role checks)." }
    },
    "d1-alrt": {
      "type": "card",
      "variant": "elevated",
      "props": { "title": "ALRT-01 · Man-overboard has two acknowledgement authorities (shapes 1+3 combined)", "content": "**Where:** `src/live/mobStore.ts` (header comment: \"No backend\"), `src/components/AlarmCenter.tsx:76-85, 223, 290-351`, `src/screens/MapScreen.tsx:398-399, 617-619`, `convex/safetyAlerts.ts:42`, `src/live/cloudSafety.ts:98`.\n\n**Mechanism:** one MOB event lives in two unsynchronized systems. The persistent emergency banner is driven purely by a client-local store whose only dismiss path is the map screen's \"Clear MOB\" — a localStorage write with no server call. The same event is *also* mirrored into the shared alerts table as a critical row, which renders as a normal alert card with its own real server-backed \"Ack\" button — stacked on the same screen, below the banner.\n\n**What an operator sees:** clearing the MOB banner leaves the alert row open in the queue and audit trail forever; acknowledging the alert row leaves the emergency banner up. Two contradictory MOB UIs at once, during the highest-stakes workflow the product has.\n\n**Fix:** one event, one authority. \"Clear MOB\" routes through the same server acknowledge mutation as the alert row, and the banner derives its presence from that row's acknowledgement state. The local store becomes a pure render cache or is deleted." }
    },
    "d1-zones": {
      "type": "card",
      "variant": "elevated",
      "props": { "title": "ZONES-P0-01 · The Live Map ignores the Geofences screen entirely (shape 3, plus duplication)", "content": "**Where:** `src/screens/MapScreen.tsx:6, 521` vs `src/screens/GeofencesScreen.tsx:825-826, 905-906`.\n\n**Mechanism:** the primary Live Map passes the raw static built-in zone import — all four zones, hardcoded enabled — straight into the map component, never filtered through the operator's enabled/disabled preferences, and passes no custom-zones prop at all. The Geofences screen's own embedded map does all of this correctly. The logic simply was never shared: two screens, two divergent wirings of the same map component.\n\n**What an operator sees:** disable a built-in zone, or draw a new custom zone, in Geofences — then switch to the Live Map, the screen that actually sits on the bridge display, and see the old zone set with your custom zone missing. The two screens disagree about what boundaries exist on the water.\n\n**Fix:** extract the zone-selection logic (enabled built-ins + custom zones) into one shared hook both screens consume. This also erases the duplicated map wiring the spec audit flagged separately." }
    },

    "disease2-section": {
      "type": "section",
      "variant": "stacked",
      "props": { "label": "DISEASE 2", "title": "Telemetry has no provenance", "subtitle": "Four findings that together mean: the moment synthetic data exists, every evidence surface would present it as real. This is why the provenance rail is plan step 3, before any parity loop." },
      "children": ["disease2-explainer", "d2-ins1", "d2-ins2", "d2-insight", "d2-sim"]
    },
    "disease2-explainer": {
      "type": "text",
      "variant": "prose",
      "props": { "content": "Disease one is about facts diverging between browser and server. Disease two is deeper: a fact the system never records at all — *where a telemetry row came from*. There is exactly one `source` field anywhere in the schema, and it belongs to an unrelated webhook cursor. No table distinguishes a row written by the real boat pipe from one written by an edge box, a backfill script, or a simulator. Today that's moot because no simulator writes to the cloud; the plan makes it existential, because the goal is precisely to pump ten scenarios of synthetic data through the production ingest. Without the discriminator, sim parity would *poison* every evidence surface as a side effect of testing it." }
    },
    "d2-ins1": {
      "type": "card",
      "variant": "elevated",
      "props": { "title": "INS-01 · Insurance reports assert \"real evidence\" with no way to know (P0)", "content": "**Where:** `convex/schema.ts:434, 490, 846` (telemetry, telemetrySamples, rentalScores — no source field anywhere), `convex/lib/insuranceReport.ts:91`, `src/screens/InsuranceScreen.tsx:282`.\n\n**Mechanism:** the insurance screen renders, verbatim: *\"Every figure is computed deterministically from real rental, maintenance and engine-bus evidence — never AI-generated.\"* Neither the source tables nor the report input type carry any field that could falsify that sentence once synthetic rows exist in the same tables.\n\n**Why it blocks parity by definition:** the acceptance contract requires synthetic data to be visibly synthetic on every evidence output. This screen structurally cannot comply.\n\n**Fix:** the `source: dtu | edge | sim` discriminator from the append-only decision, threaded through the report input, with the screen's claim gated on it — real-only reports keep the sentence; anything else labels or excludes." }
    },
    "d2-ins2": {
      "type": "card",
      "variant": "elevated",
      "props": { "title": "INS-02 · Partial evidence renders identically to full evidence (P0)", "content": "**Where:** `convex/lib/insuranceReport.ts:129-130, 625, 658, 679, 776-780` versus the screen, which never reads these fields (zero matches for coverage/disposition in `InsuranceScreen.tsx`).\n\n**Mechanism:** the report engine is honest — it tracks which of the four weighted risk factors (behavior, incidents, compliance, faults) were actually available, reallocates weights among only those, and exposes `coverage` and `disposition` on every report. The screen throws that honesty away: it renders the band and the score and never shows the coverage.\n\n**What an underwriter sees:** a \"Preferred\" risk band computed from two of four factors is pixel-identical to one backed by complete evidence. Only the fully-empty case surfaces (as an insufficient-data chip); the partial case is silent.\n\n**Fix:** render the fields that already exist — an \"N of 4 risk factors available\" chip on the report card, consistent with the evidence-floor gating the fleet-health surface already does." }
    },
    "d2-insight": {
      "type": "card",
      "variant": "elevated",
      "props": { "title": "INSIGHT-1 · The schema gap feeds every intelligence surface blind (P0)", "content": "**Where:** `convex/fleetHealth.ts` (~line 952 query), `convex/optimizer.ts`, `convex/ml.ts`, `convex/telemetryHistory.ts:426` (the ML training row type) — all read telemetry generically with no provenance in the row shape.\n\n**Mechanism:** the same missing field, seen from the consumers' side. Fleet health's revenue-at-risk queue, the optimizer's utilization report, cross-club network benchmarks, and the ML training pipeline all consume telemetry rows with no way to include or exclude by origin.\n\n**Why it matters beyond insurance:** ML training on blended sim+real data silently corrupts models; a benchmark comparing a real club against a simulated one is fiction presented as comparison.\n\n**Fix:** this is why the fix lands WITH the append-only refactor rather than as a UI patch — one discriminator on the history event, propagated to the training row type and every scorer, is one migration instead of five retrofits. The dataset authority contract then has each consumer declare its permitted sources and fail closed otherwise." }
    },
    "d2-sim": {
      "type": "card",
      "variant": "elevated",
      "props": { "title": "SIM-1 · No path exists from any generator to cloud Convex (P0 — a build, not a bug)", "content": "**Where:** `boat/demo.ts:9-23`, `boat/gateway.ts:16, 38` — the only synthetic generators in the repo POST to a local consumer-era boat server on port 8088. Zero Convex imports anywhere under `boat/`; no simulator directory exists in the fleet dashboard.\n\n**Mechanism:** the existing demo tooling belongs to the pre-pivot consumer prototype's architecture. The cloud dashboard has never had a synthetic feed; every intelligence surface has only ever been exercised by real ingest or hand-seeded rows. The simulator workstream is at phase zero.\n\n**Consequence for the plan:** step 3 is a *build*, not a wire-up — generator, sim-lane service key, manifest/ack receipts, all of it. This is the largest single work item between here and the first oracle run, which is why it owns its own plan step with step 4 explicitly forbidden from starting before it exists.\n\n**Fix shape:** the generator speaks the same wire contract as production ingest (`tracking.reportTelemetry`, batch variant for replay bursts) so the sim exercises the real path — never a parallel seam that could pass while production ingest fails." }
    },

    "structural-section": {
      "type": "section",
      "variant": "stacked",
      "props": { "label": "STRUCTURAL", "title": "Scheduled, not blocking", "subtitle": "Three more findings that shape the work but don't gate the first slices." },
      "children": ["underway-table-intro", "underway-table", "structural-cards"]
    },
    "underway-table-intro": {
      "type": "text",
      "variant": "prose",
      "props": { "content": "**Three screens, three definitions of \"underway.\"** The same boat at the same instant can count as underway on the dashboard KPI and not on the dispatch board, because each surface reimplements the predicate with different constants. This promotes into the parity loop because sim scenarios exercising edge speeds and staleness will trip the disagreement immediately — and the oracle can't define one expected state while the app holds three definitions. Fix: one shared `isUnderway(boat, now)` predicate all three call." }
    },
    "underway-table": {
      "type": "data-table",
      "props": {
        "columns": [
          { "key": "surface", "label": "Surface" },
          { "key": "where", "label": "Where" },
          { "key": "speed", "label": "Speed threshold" },
          { "key": "fresh", "label": "Freshness window" },
          { "key": "engine", "label": "Engine-running counts?" }
        ],
        "rows": [
          { "surface": "Fleet readiness briefing", "where": "src/lib/briefing.ts:164-172", "speed": "≥ 0.5 kn", "fresh": "60 s", "engine": { "text": "Yes (OR fallback)", "status": "info" } },
          { "surface": "Dashboard KPI", "where": "src/screens/DashboardScreen.tsx:35-80", "speed": "≥ 0.5 kn", "fresh": "60 s", "engine": { "text": "No", "status": "warn" } },
          { "surface": "Dispatch live signal", "where": "src/screens/DispatchScreen.tsx:121-149", "speed": { "text": "≥ 1 kn (2×)", "status": "warn" }, "fresh": { "text": "600 s (10×)", "status": "warn" }, "engine": { "text": "Yes", "status": "info" } }
        ]
      }
    },
    "structural-cards": {
      "type": "section",
      "variant": "comparison",
      "props": { "label": "", "title": "" },
      "children": ["struct-nav-card", "struct-ds-card"]
    },
    "struct-nav-card": {
      "type": "card",
      "variant": "default",
      "props": { "title": "🔓 Admin navigation is unguarded (P1)", "content": "The eight-item admin nav group (`src/nav.ts:22-33`) renders unconditionally for every role (`src/components/Sidebar.tsx:45-47`); a viewer can browse into Crew, Devices, Settings and see everything readable. Server-side write gates still hold, but read visibility is unrestricted. The admin-flavor decision names the exact fix — one rank check hiding the group below admin — small, independently shippable, accepted but unbuilt." }
    },
    "struct-ds-card": {
      "type": "card",
      "variant": "default",
      "props": { "title": "🔱 The design-system fork (P0, structural)", "content": "The repo's design-system package defines 10+ primitives (Card, Modal, StatTile, Toggle, …); the fleet dashboard imports none of it — its own 532-line `src/components/ui.tsx` reimplements the same names on a different token substrate (inline styles + CSS-var indirection, by design). Every visual or accessibility fix must land twice, and the sets drift silently. Ruling from the audit: decide the substrate before ANY component convergence. It shapes the roadmap but deliberately does not block parity." }
    },

    "slice-section": {
      "type": "section",
      "variant": "default",
      "props": { "label": "EMITTED", "title": "The first slice is on the runway", "subtitle": "Emitted per your go, validated READY by the deterministic checker (5 claims, 5 failure hypotheses, 5 fitness functions, zero diagnostics)." },
      "children": ["slice-card", "slice-holds"]
    },
    "slice-card": {
      "type": "card",
      "variant": "glass",
      "props": { "title": "✅ wire-deploy-path (.lev/pm/tasks/suntiq-fleet-wire-deploy-path/)", "content": "**Outcome:** the SuntiQ staging URL serves the fleet dashboard in cloud mode against the suntiq-fleet Convex deployment — Clerk gate up, empty-org state for a fresh user, zero mock data — while fleet.argomarine.ai runs untouched.\n\n**Built-in paranoia, straight from the session's own lessons:** the first fitness function requires the Convex deployment to be named by live CLI output *before* any env references it (the fabricated-id lesson); another catches the silent mock-mode false-green (build-time env vars missing → demo data renders and looks alive); another verifies the mirror's history stays pure split hashes.\n\n**Forbidden:** prod Clerk or DNS (open question), source-code fixes smuggled into deploy wiring, anything touching the droplet or the pilot stack." }
    },
    "slice-holds": {
      "type": "card",
      "variant": "callout",
      "props": { "variant": "warning", "title": "It starts the moment you hand over two things", "content": "1) SuntiQ **Clerk dev keys** — app creation needs your account; the JWT template must be named `convex`.\n2) Nothing else. The deployment-verification step is inside the slice itself." }
    },

    "next-section": {
      "type": "section",
      "variant": "stacked",
      "props": { "label": "NEXT", "title": "Open threads" },
      "children": ["next-prose"]
    },
    "next-prose": {
      "type": "text",
      "variant": "prose",
      "props": { "content": "**One ruling open:** Clerk dev-only vs standing up prod (needs the fleet.suntiq.ai DNS call).\n\n**One spike queued:** Format 5 on box #1, plus the serial last-4 collision inventory when the 20 boxes arrive.\n\n**One reconcile due:** the 22 reverse-engineered specs were written by parallel audit agents pinned to two different commits; the first spec-sync run must bring them to one baseline before specs are cited as current.\n\n**One dependency unchanged:** cutover still waits on Sunny's data export and the droplet SSH key — unrelated to this package but on the same calendar.\n\nEverything on this page is recorded in canon: rulings and fitness functions in the plan, the DTU evidence in the data-scale design, the enrichment and rulings ledger in the workstream file, and the task packet on disk validating READY." }
    }
  }
}
