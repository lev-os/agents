# Validation Gates for .agents
# Generated by: lev install
# Detected stacks: none
#
# Customize gates for your project. Remove or modify as needed.

version: "1.0"
name: .agents-validation-gates

checks:
  # Build validation
  build:
    status: enforced
    command: echo "no build command configured"
    description: Configure a build command for your project
    status: declared

  # Test validation
  tests:
    status: enforced
    command: echo "no test command configured"
    status: declared
    description: All tests pass

  # Lint validation
  lint:
    status: declared
    command: echo "no lint command configured"
    description: Configure a linter for your project

  # XDG compliance
  xdg_compliance:
    status: declared
    description: No runtime state under legacy hidden folders
    invalid_paths:
      - ~/.lev/

