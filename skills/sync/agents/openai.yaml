interface:
  display_name: "Sync"
  short_description: "Checkpoint submodules, worktrees, and main"
  default_prompt: "Use $sync to checkpoint dirty submodules, worktrees, and main with merge-based pulls and no PR flow."
