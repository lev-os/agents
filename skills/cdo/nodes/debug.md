Fixed 7-turn RCA: reproduce → isolate → trace (parallel) → hypothesize → verify → fix → validate.
3-FIX RULE: If 3+ fixes failed, STOP. This is architectural, not a bug.
Report to user what each attempt revealed about the design.
